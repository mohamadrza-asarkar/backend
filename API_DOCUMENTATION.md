# 📖 راهنمای کامل و مستندات جامع APIهای فروشگاه برنج (Rice Store Backend)

این مستندات برای تیم توسعه فرانت‌اند (Frontend) به منظور اتصال راحت، ایمن و استاندارد به سرور بک‌اند تهیه شده است.

* **آدرس پایه سرور (Base URL):** `https://ais-dev-rpvkewlvjilhjnoamjgjvq-240344892228.europe-west1.run.app`
* **آدرس محیط اشتراکی:** `https://ais-pre-rpvkewlvjilhjnoamjgjvq-240344892228.europe-west1.run.app`

---

## 🔒 قوانین کلی امنیت و هدرها (Headers & Auth Rule)

تمامی روت‌هایی که به دسترسی کاربر یا ادمین نیاز دارند، **منحصراً** توکن را از هدر (Header) دریافت می‌کنند.
* برای روت‌های حفاظت‌شده، باید هدر زیر ارسال شود:
  ```http
  Authorization: Bearer <YOUR_JWT_TOKEN>
  ```
  *(یا به صورت کلید ساده `token` در هدر: `token: <YOUR_JWT_TOKEN>`)*
* **مهم:** از فرستادن فیلدهای ادمین یا توکن درون بدنه درخواست (`body`) یا آدرس صفحه (`query`) برای اهداف احراز هویت خودداری کنید؛ زیرا سرور جهت افزایش امنیت آن‌ها را نادیده می‌گیرد.

---

## 👥 ۱. احراز هویت و مدیریت کاربران (`/api/auth`)

### ثبت‌نام کاربر جدید
ثبت‌نام عادی کاربران در سیستم.
* **متد:** `POST`
* **آدرس:** `/api/auth/register`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "name": "محمدرضا",
    "phone": "09123456789",
    "password": "my_secure_password_123",
    "address": "گیلان، رشت، میدان گلسار"
  }
  ```
* **پاسخ سرور در صورت موفقیت (Status 201):**
  ```json
  {
    "user": {
      "_id": "651a2b3c4d5e6f7a8b9c0d1e",
      "name": "محمدرضا",
      "phone": "09123456789",
      "address": "گیلان، رشت، میدان گلسار",
      "role": "user",
      "avatar": "",
      "isActive": true,
      "createdAt": "2026-09-22T06:12:00.000Z",
      "updatedAt": "2026-09-22T06:12:00.000Z"
    },
    "token": "eyJhbGciOiJIUzI1NiIsIn..."
  }
  ```

### ورود به سیستم
دریافت توکن دسترسی برای کاربر ثبت‌نام شده.
* **متد:** `POST`
* **آدرس:** `/api/auth/login`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "phone": "09123456789",
    "password": "my_secure_password_123"
  }
  ```
* **پاسخ سرور در صورت موفقیت (Status 200):**
  ```json
  {
    "user": {
      "_id": "651a2b3c4d5e6f7a8b9c0d1e",
      "name": "محمدرضا",
      "phone": "09123456789",
      "address": "گیلان، رشت، میدان گلسار",
      "role": "user",
      "avatar": "",
      "isActive": true
    },
    "token": "eyJhbGciOiJIUzI1NiIsIn..."
  }
  ```

### دریافت پروفایل من (نیاز به توکن)
* **متد:** `GET`
* **آدرس:** `/api/auth/me`
* **پاسخ سرور (Status 200):**
  ```json
  {
    "user": {
      "_id": "651a2b3c4d5e6f7a8b9c0d1e",
      "name": "محمدرضا",
      "phone": "09123456789",
      "address": "گیلان، رشت، میدان گلسار",
      "role": "user",
      "avatar": "",
      "isActive": true
    }
  }
  ```

### ویرایش اطلاعات پروفایل (نیاز به توکن)
* **متد:** `PUT`
* **آدرس:** `/api/auth/profile`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "name": "نام جدید",
    "address": "آدرس تحویل جدید",
    "avatar": "https://link-to-image.com/avatar.jpg"
  }
  ```
* **پاسخ سرور (Status 200):**
  ```json
  {
    "user": {
      "_id": "651a2b3c4d5e6f7a8b9c0d1e",
      "name": "نام جدید",
      "phone": "09123456789",
      "address": "آدرس تحویل جدید",
      "role": "user",
      "avatar": "https://link-to-image.com/avatar.jpg",
      "isActive": true
    }
  }
  ```

---

## 🌾 ۲. محصولات و سیستم شگفت‌انگیز (`/api/products`)

### دریافت لیست تمام محصولات (عمومی)
* **متد:** `GET`
* **آدرس:** `/api/products`
* **پاسخ سرور (Status 200):**
  ```json
  [
    {
      "_id": "652a11112222333344445555",
      "name": "برنج هاشمی درجه یک گیلان",
      "description": "برنج دانه بلند با عطر بی نظیر و پخت عالی",
      "price": 110000,
      "originalPrice": 120000,
      "discountPercent": 8,
      "isAmazing": true,
      "amazingExpiresAt": "2026-09-30T00:00:00.000Z",
      "isAvailable": true,
      "countInStock": 150,
      "image": "/uploads/products/hashemi.jpg",
      "reviews": [],
      "createdAt": "2026-09-21T22:00:00.000Z"
    }
  ]
  ```

### دریافت لیست محصولات شگفت‌انگیز (عمومی)
* **متد:** `GET`
* **آدرس:** `/api/products/amazing` یا `/api/amazing-products`
* **پاسخ سرور (Status 200):** آرایه‌ای از محصولات که فیلد `isAmazing` آنها `true` است.

### جستجوی محصولات (عمومی)
* **متد:** `GET`
* **آدرس:** `/api/products/search?q=برنج`
* **پاسخ سرور (Status 200):** آرایه‌ای از نتایج منطبق بر کلیدواژه جستجو.

### ایجاد محصول جدید (فقط ادمین - آپلود فایل تصویری)
جهت ایجاد محصول، درخواست باید به صورت `multipart/form-data` ارسال شود تا فایل آپلود شود.
* **متد:** `POST`
* **آدرس:** `/api/products`
* **فرمت ارسالی (FormData):**
  - فیلد متنی: `name`
  - فیلد متنی: `description`
  - فیلد متنی: `price` (عدد)
  - فیلد متنی: `originalPrice` (اختیاری)
  - فیلد متنی: `discountPercent` (اختیاری)
  - فیلد متنی: `isAmazing` (`true` / `false`)
  - فیلد متنی: `amazingExpiresAt` (تاریخ ISO اختیاری)
  - فیلد متنی: `countInStock` (عدد)
  - فیلد فایل: `image` (فایل تصویری محصول)
* **پاسخ سرور در صورت موفقیت (Status 201):** شیء محصول جدید ایجاد شده همراه با آدرس تصویر بر روی سرور در فیلد `image`.

---

## 🛒 ۳. مدیریت سبد خرید آنلاین (`/api/cart`)
*تمامی روت‌های این بخش نیاز به توکن دارند و با تغییر اکانت کاربر در هر دستگاهی همگام می‌مانند.*

### دریافت سبد خرید
* **متد:** `GET`
* **آدرس:** `/api/cart`
* **پاسخ سرور (Status 200):**
  ```json
  {
    "_id": "653a98765432109876543210",
    "user": "651a2b3c4d5e6f7a8b9c0d1e",
    "items": [
      {
        "product": {
          "_id": "652a11112222333344445555",
          "name": "برنج هاشمی درجه یک گیلان",
          "price": 110000,
          "image": "/uploads/products/hashemi.jpg"
        },
        "quantity": 3,
        "_id": "653a98765432109876543211"
      }
    ],
    "createdAt": "2026-09-21T23:00:00.000Z"
  }
  ```

### افزودن یا ویرایش تعداد محصول در سبد
* **متد:** `POST`
* **آدرس:** `/api/cart`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "productId": "652a11112222333344445555",
    "quantity": 5
  }
  ```
* **پاسخ سرور (Status 200):** شیء به‌روز شده‌ی سبد خرید کاربر.

### حذف یک محصول خاص از سبد
* **متد:** `DELETE`
* **آدرس:** `/api/cart/652a11112222333344445555`
* **پاسخ سرور (Status 200):**
  ```json
  {
    "success": true,
    "message": "آیتم از سبد خرید حذف شد",
    "cart": { ...سبد خرید به‌روز شده... }
  }
  ```

### خالی کردن کامل سبد خرید
* **متد:** `DELETE`
* **آدرس:** `/api/cart`
* **پاسخ سرور (Status 200):** سبد خرید خالی شده کاربر.

---

## 📦 ۴. مدیریت و ثبت سفارشات (`/api/orders`)

### ثبت سفارش جدید (همراه با آپلود فیش بانکی اختیاری)
سفارشات می‌توانند به همراه آپلود تصویر رسید واریز بانکی به صورت `multipart/form-data` ارسال شوند.
* **متد:** `POST`
* **آدرس:** `/api/orders`
* **فرمت ارسالی (FormData):**
  - فیلد متنی: `name` (نام تحویل‌گیرنده)
  - فیلد متنی: `phone` (شماره موبایل تماس)
  - فیلد متنی: `address` (آدرس دقیق گیرنده)
  - فیلد متنی: `postalCode` (کد پستی ده رقمی)
  - فیلد متنی: `items` (یک آرایه به صورت رشته JSON به شکل زیر):
    `"[{\"product\":\"652a11112222333344445555\",\"name\":\"برنج هاشمی\",\"price\":110000,\"quantity\":2}]"`
  - فیلد متنی: `totalPrice` (عدد مجموع قیمت کل)
  - فیلد فایل: `receipt` (فایل تصویری اسکرین شات فیش واریز)
* **پاسخ سرور در صورت موفقیت (Status 201):**
  ```json
  {
    "_id": "654b99887766554433221100",
    "name": "محمدرضا",
    "phone": "09123456789",
    "address": "گیلان، رشت، میدان گلسار",
    "postalCode": "1234567890",
    "postTrackingCode": "",
    "state": "pending",
    "paymentStatus": "submitted",
    "paymentReceipt": "/uploads/receipts/receipt-1695325000000.jpg",
    "paymentReceiptDate": "2026-09-22T06:30:00.000Z",
    "adminNote": "",
    "products": [
      {
        "product": "652a11112222333344445555",
        "name": "برنج هاشمی",
        "price": 110000,
        "quantity": 2,
        "_id": "654b99887766554433221101"
      }
    ],
    "totalPrice": 220000,
    "createdAt": "2026-09-22T06:30:00.000Z",
    "updatedAt": "2026-09-22T06:30:00.000Z"
  }
  ```

### دریافت سفارشات کاربر لاگین شده (نیاز به توکن)
* **متد:** `GET`
* **آدرس:** `/api/orders`
* **پاسخ سرور (Status 200):** آرایه‌ای از سفارشات ثبت‌شده توسط کاربر جاری.

### آپلود/ویرایش فیش پرداخت برای سفارش از پیش ثبت‌شده
در صورتی که کاربر سفارش را ثبت کرده ولی فیش را بعداً ارسال می‌کند.
* **متد:** `POST` یا `PUT`
* **آدرس:** `/api/orders/:id/receipt`
* **فرمت ارسالی (FormData):**
  - فیلد فایل: `receipt` (تصویر جدید فیش)
* **پاسخ سرور (Status 200):** شیء سفارش به‌روزرسانی شده.

### پیگیری عمومی وضعیت سفارش (بدون نیاز به توکن)
جهت دسترسی مهمان به وضعیت ارسال بسته از طریق شناسه سفارش یا کد رهگیری پستی.
* **متد:** `GET`
* **آدرس:** `/api/orders/track/:postTrackingCode`
* **پاسخ سرور در صورت موفقیت (Status 200):**
  ```json
  {
    "_id": "654b99887766554433221100",
    "name": "محمدرضا",
    "state": "shipped",
    "postTrackingCode": "1098542365879",
    "adminNote": "بسته شما تحویل اداره پست رشت گردید.",
    "totalPrice": 220000,
    "createdAt": "2026-09-22T06:30:00.000Z"
  }
  ```

---

## 💬 ۵. نظرات کاربران (`/api/reviews`)

### دریافت نظرات بر اساس محصول
* **متد:** `GET`
* **آدرس:** `/api/reviews?productId=652a11112222333344445555`
* **پاسخ سرور (Status 200):**
  ```json
  [
    {
      "_id": "655c11223344556677889900",
      "productId": "652a11112222333344445555",
      "sender": "علی احمدی",
      "comment": "عالی و خوشپخت بود ممنون",
      "rating": 5,
      "adminReply": "سلام، نوش جان و رضایت شما مایه افتخار ماست.",
      "createdAt": "2026-09-22T05:00:00.000Z"
    }
  ]
  ```

### ثبت نظر جدید برای محصول (نیاز به توکن)
* **متد:** `POST`
* **آدرس:** `/api/reviews`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "productId": "652a11112222333344445555",
    "rating": 5,
    "comment": "پیشنهاد میکنم حتماً تست کنید."
  }
  ```
* **پاسخ سرور (Status 201):** شیء نظر ثبت‌شده در سیستم.

---

## 🖼️ ۶. اسلایدر صفحه اصلی (`/api/slides`)

### دریافت اسلایدهای هدر صفحه اول (عمومی)
* **متد:** `GET`
* **آدرس:** `/api/slides`
* **پاسخ سرور (Status 200):**
  ```json
  [
    {
      "_id": "656d00010002000300040005",
      "image": "/uploads/slides/slide-1695325100000.jpg",
      "createdAt": "2026-09-21T18:00:00.000Z"
    }
  ]
  ```

---

## 👑 ۷. پنل مدیریت و ادمین (`/api/admin`)
*(تمامی روت‌های این بخش نیاز مبرم به توکن با نقش ادمین در هدر دارند)*

### دریافت آمار داشبورد ادمین
* **متد:** `GET`
* **آدرس:** `/api/admin/dashboard`
* **پاسخ سرور (Status 200):**
  ```json
  {
    "totalSales": 14250000,
    "ordersCount": 42,
    "usersCount": 89,
    "latestOrders": [ ...لیست ۵ سفارش اخیر به همراه مشخصات کامل... ]
  }
  ```

### لیست تمامی کاربران سیستم
* **متد:** `GET`
* **آدرس:** `/api/admin/users`
* **پاسخ سرور (Status 200):** آرایه‌ای از اطلاعات تمامی کاربران دیتابیس.

### ارتقا یا تغییر نقش کاربر
* **متد:** `PUT`
* **آدرس:** `/api/admin/users/:id/role`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "role": "admin" // یا "user"
  }
  ```

### بررسی و تأیید/رد واریزی و فیش پرداخت سفارش
ادمین از این بخش می‌تواند فیش پرداخت را بررسی، سفارش را تایید، وضعیت بسته را آپدیت و کد رهگیری پستی صادر کند.
* **متد:** `PUT`
* **آدرس:** `/api/orders/:id/verify-payment`
* **نوع داده دریافتی (JSON):**
  ```json
  {
    "status": "approved", // 'approved' یا 'rejected'
    "state": "processing", // یا 'shipped', 'delivered', 'cancelled'
    "adminNote": "واریزی تایید شد. فردا صبح ارسال می شود",
    "postTrackingCode": "104587954124" // کد رهگیری اداره پست ایران
  }
  ```
* **پاسخ سرور (Status 200):** شیء سفارش به‌روز شده با اطلاعات ثبت‌شده ادمین.

---

### 💡 راهنمایی‌های کلیدی فرانت‌اند (Frontend Tips):
1. **آپلود تصاویر:** برای روت‌های ایجاد محصول جدید، آپلود فیش بانکی سفارش و آپلود اسلایدها هدر `Content-Type` را بر روی `multipart/form-data` تنظیم کنید و بگذارید مرورگر فولدربندی مرزهای فرستادن تصویر را به طور خودکار انجام دهد.
2. **مدیریت توکن:** توکن دریافتی پس از ورود یا ثبت‌نام را در `localStorage` یا `Cookie` ذخیره نموده و به عنوان هدر احراز هویت در رکوئست‌های بعدی تنظیم نمایید.
3. **کد وضعیت‌ها (StatusCodes):**
   - **401 Unauthorized:** توکن ارسال نشده یا منقضی است. کاربر باید مجدد لاگین کند.
   - **403 Forbidden:** کاربر به روت ادمین دسترسی ندارد.
   - **400 Bad Request:** داده‌های ورودی اشتباه هستند یا ناقص ارسال شده‌اند.
   - **404 Not Found:** روت یا داده درخواستی یافت نشد.
